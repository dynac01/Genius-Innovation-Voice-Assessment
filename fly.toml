# fly.toml app configuration file generated for genius-innovation-voice-assessment on 2026-08-04T23:38:43Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'genius-innovation-voice-assessment'
primary_region = 'ewr'

[build]
  dockerfile = 'Dockerfile'

[env]
  LLM_PROVIDER = 'anthropic'
  STT_PROVIDER = 'deepgram'
  TTS_PROVIDER = 'deepgram'

[http_service]
  internal_port = 8787
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = 'connections'
    hard_limit = 25
    soft_limit = 20

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/health'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
